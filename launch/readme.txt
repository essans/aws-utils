Launch directory organized based on family of instances

For each config file found in these family directories there are attributes that likely need
updating for a different user/account:
 - tag names
 - ssh keyname
 - IAM instance profile
 - Network interface subnet
 - Network interace security group (sg)